#include<stdio.h>
int main(void) // void explicitly tells readers that main takes no arguments, however, the bracket can be left empty too
{
    printf("Hello\n");// \n is used to move the cursor to the next line
    printf("World");
}